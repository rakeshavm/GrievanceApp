# GrievanceApp
React-Native app<br>
ML based mobile app which helps to seamlessly register neighbourhood complaints and monitor the progress.
The clicked images are classified according to complaints and forwarded to government authorities.
# Getting started

To get the app running locally:

- Clone this repo
- `npm install` to install all required dependencies
- `npm run android` to start build for android app
